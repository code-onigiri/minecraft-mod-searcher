# minecraft-mod-searcher

MinecraftのModを複数プラットフォーム横断で検索するWebアプリケーションです。

## セットアップ

1. 依存関係をインストール
2. `.env.example` を `.env.local` にコピーしてSupabaseの値を設定
3. 開発サーバーを起動

## スクリプト

- `bun dev`: 開発サーバー
- `bun run build`: ビルド
- `bun run test`: テスト
- `bun run lint`: Lint
